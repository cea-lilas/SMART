Installation
============

The installation of SMART is straightforward. You can install it directly from the GitHub repository using pip. Here are the steps to follow:

.. code-block:: bash

   git clone git@github.com:cea-lilas/SMART.git
   cd SMART
   pip install .
